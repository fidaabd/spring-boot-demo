spring.application.name=cabinet

# Base de données — nom corrigé : cabinet
spring.datasource.url=jdbc:mysql://localhost:3306/cabinet?useSSL=false&serverTimezone=Africa/Tunis&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Ghofrane@2026
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Flyway désactivé (tables déjà créées manuellement)
spring.flyway.enabled=false

# Serveur
server.port=8080

# Thymeleaf
spring.thymeleaf.cache=false